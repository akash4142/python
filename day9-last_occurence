list = [12,32,43,12,32,54,65,12]
size = len(list)
target=12


def last_occurence(arr,size,target):
    index=-1
    for i in range(0,size):
        if(arr[i]==target):
            index=i;
    return index;
    
index=last_occurence(list,size,target)
print('The last index is at : ',index)
